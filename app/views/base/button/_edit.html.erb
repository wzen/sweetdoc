<%
   link_url = '' if link_url.nil?
   params = {} if params.nil?
%>
<%= form_tag link_url do  %>
    <% params.each do |k, v| %>
        <input type="hidden" name="<%= k %>" value="<%= v %>">
    <% end %>
    <%= button_tag(class:'edit_button image_button', style: 'margin-top: 5px;margin-bottom: 5px') do %>
        <%= image_tag 'etc/edit.png', size: '24x24' %>
    <% end %>
<% end %>