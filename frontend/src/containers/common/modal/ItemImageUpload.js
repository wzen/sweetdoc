import {connect} from 'react-redux';
import ItemImageUpload from '../../../components/common/modal/ItemImageUpload';

const mapDispatchToProps = (dispatch) => {
  return {

  }
};

export default connect(
  null,
  mapDispatchToProps
)(ItemImageUpload);