import {connect} from 'react-redux';
import CreateUserCode from '../../../components/common/modal/CreateUserCode';

const mapDispatchToProps = (dispatch) => {
  return {

  }
};

export default connect(
  null,
  mapDispatchToProps
)(CreateUserCode);