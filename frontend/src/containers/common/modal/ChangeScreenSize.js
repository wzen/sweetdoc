import {connect} from 'react-redux';
import ChangeScreenSize from '../../../components/common/modal/ChangeScreenSize';

const mapDispatchToProps = (dispatch) => {
  return {

  }
};

export default connect(
  null,
  mapDispatchToProps
)(ChangeScreenSize);