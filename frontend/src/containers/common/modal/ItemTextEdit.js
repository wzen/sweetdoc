import {connect} from 'react-redux';
import ItemTextEdit from '../../../components/common/modal/ItemTextEdit';

const mapDispatchToProps = (dispatch) => {
  return {

  }
};

export default connect(
  null,
  mapDispatchToProps
)(ItemTextEdit);