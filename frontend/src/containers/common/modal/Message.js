import {connect} from 'react-redux';
import Message from '../../../components/common/modal/Message';

const mapDispatchToProps = (dispatch) => {
  return {

  }
};

export default connect(
  null,
  mapDispatchToProps
)(Message);