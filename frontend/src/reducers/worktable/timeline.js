import { combineReducers } from 'redux';

const timeline = (state, action) => {
  switch(action.type) {
    case 'CREATE_ITEM':
      return state;
    default:
      return state;
  }
};

export default timeline;